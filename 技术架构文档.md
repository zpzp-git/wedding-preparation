# 备婚规划与方案对比平台技术架构 V1.0

## 1. 技术目标

本项目为个人使用的备婚规划与方案对比平台。

系统特点：

* 业务规模较小
* 数据量较小
* 数据关系清晰
* 后端计算逻辑不复杂
* 对界面体验要求较高
* 需要大量卡片、树、方案切换、对比视图、抽屉、弹窗等现代交互
* 后续可能持续通过 AI Coding 方式迭代
* 希望技术栈简单，但不能限制未来 UI 能力

因此整体采用：

> **Next.js 全栈模块化单体架构**

不拆分独立前后端项目，不引入 Java、.NET 等独立后端服务，也不引入微服务、Redis、消息队列等当前没有必要的基础设施。

核心原则：

> **部署简单，代码分层，前端体验优先，技术能力留有余量。**

---

# 2. 总体技术架构

```text
                 Browser
                    │
                    ▼
┌────────────────────────────────────┐
│              Next.js               │
│                                    │
│  ┌──────────────────────────────┐  │
│  │          React UI            │  │
│  │                              │  │
│  │ shadcn/ui                    │  │
│  │ Tailwind CSS                 │  │
│  │ Motion                       │  │
│  │ Lucide Icons                 │  │
│  └──────────────┬───────────────┘  │
│                 │                  │
│  ┌──────────────▼───────────────┐  │
│  │       Next.js Server         │  │
│  │                              │  │
│  │ Server Components            │  │
│  │ Server Actions               │  │
│  │ Route Handlers               │  │
│  │ Business Services            │  │
│  └──────────────┬───────────────┘  │
│                 │                  │
│             Drizzle ORM            │
└─────────────────┬──────────────────┘
                  │
                  ▼
               SQLite
```

系统整体保持一个应用、一个代码仓库、一个数据库。

---

# 3. 核心技术栈

## 3.1 编程语言

采用：

**TypeScript**

整个项目统一使用 TypeScript，包括：

* React 前端组件
* 页面逻辑
* 服务端业务逻辑
* 数据访问
* 数据模型
* 参数校验

不额外引入其他后端语言。

这样可以降低项目复杂度，同时充分利用现代 React / Next.js 生态。

---

# 4. 全栈框架

采用：

**Next.js 16**

Next.js 负责：

* 页面
* 路由
* React Server Components
* Client Components
* 服务端逻辑
* Server Actions
* HTTP API
* 数据获取
* 构建
* 部署

项目采用：

**App Router**

不使用旧 Pages Router。

整体不再设计：

```text
frontend
+
backend
```

两套独立工程。

而采用：

```text
Next.js
│
├── UI
├── 页面
├── 服务端
├── 业务逻辑
└── 数据访问
```

一个工程完成整个系统。

---

# 5. 前端框架

采用：

**React 19**

React 作为 Next.js 的 UI 基础。

主要负责：

* 婚礼项目树
* 方案卡片
* 方案切换
* 对比页面
* 资源选择
* 表单
* Dashboard
* 动态交互
* 页面状态

不额外引入 Vue。

---

# 6. UI 技术体系

UI 是本项目重点。

不采用传统后台管理系统风格，而采用现代 SaaS / Personal Workspace 风格。

核心 UI 技术采用：

```text
React
+
shadcn/ui
+
Tailwind CSS 4
+
Motion
```

---

# 7. shadcn/ui

基础组件体系采用：

**shadcn/ui**

主要提供：

* Button
* Card
* Dialog
* Drawer
* Sheet
* Tabs
* Select
* Combobox
* Popover
* Tooltip
* Dropdown
* Form
* Table
* Command
* Calendar
* Sidebar
* Chart
* Skeleton
* Toast / Sonner

shadcn/ui 的特点是：

> 组件代码直接存在于自己的项目中，而不是完全封装在第三方组件库中。

因此后续可以直接修改：

* 布局
* 样式
* 动效
* 圆角
* 阴影
* 渐变
* 间距
* 交互

不会像一些传统组件库一样受到大量默认样式限制。

这是本项目能够持续做漂亮 UI 的重要基础。

---

# 8. Tailwind CSS

采用：

**Tailwind CSS 4**

负责整体视觉设计体系。

包括：

* 色彩
* 间距
* 字体
* 圆角
* 阴影
* 渐变
* 毛玻璃
* 响应式布局
* Hover
* Focus
* 暗色模式
* 动态状态

平台不采用传统 Bootstrap 风格。

整体视觉方向建议：

> **现代、轻盈、浪漫但克制、有设计感，不做传统管理后台。**

例如可使用：

* 大面积留白
* 柔和渐变
* 半透明卡片
* 微弱 Blur
* 大圆角卡片
* 层级阴影
* 动态数字
* 悬浮反馈
* Smooth Transition

但避免过度：

* 大量彩虹渐变
* 大量闪烁
* 大量玻璃拟态
* 复杂粒子背景
* 无意义动画

目标是：

> **精致 > 花哨。**

---

# 9. 动画体系

采用：

**Motion for React**

Motion 即原 Framer Motion 的现代版本。

主要用于：

### 卡片交互

例如方案卡片：

```text
Hover
↓
轻微浮起
↓
阴影变化
↓
边框渐变
```

### 方案切换

例如：

```text
方案 A
   ↓
方案 B
```

价格、卡片选择状态平滑过渡。

### 页面布局变化

例如项目树展开、收起时：

```text
婚庆
 ├─ 主持
 ├─ 摄影
 └─ 摄像
```

平滑展开。

### 整体方案对比

方案 A / 方案 B 切换时：

* 金额变化动画
* 差异项高亮
* 卡片重新排序动画

### Drawer / Modal

使用平滑进入、退出动画。

Motion 原生支持 React，并提供布局动画、手势、Hover、Drag、滚动动画等能力。

因此未来即使需要更加复杂的交互效果，也不需要更换前端技术栈。

---

# 10. 图标体系

采用：

**Lucide React**

用于：

* 菜单
* 项目
* 状态
* 操作按钮
* 资源类型
* 提示

整体保持统一线性图标风格。

不混用多个不同风格的图标库。

---

# 11. 图表与数据可视化

简单数据图表优先使用：

**shadcn/ui Chart**

其底层可结合成熟 React 图表能力。

主要用于：

* 婚礼费用构成
* 各大类费用占比
* 整体方案价格对比
* 项目完成情况

第一版避免引入大型数据可视化平台。

---

# 12. 页面设计能力预留

前端技术栈必须保证未来可以实现以下视觉效果，而无需更换架构。

### 项目树

支持：

* 展开 / 收起
* 自定义图标
* 状态标记
* Hover
* Drag & Drop 排序
* 自定义项目
* 动画展开

### 方案卡片

支持：

```text
┌────────────────────────┐
│  摄影方案 A        ♥    │
│                        │
│  XX摄影工作室           │
│                        │
│  ¥ 3,800               │
│                        │
│  全天跟拍 · 底片全送    │
│                        │
│       当前选择 ✓        │
└────────────────────────┘
```

支持：

* 横向滚动
* 卡片选择
* Hover
* 高亮
* 动态切换
* 展开详情

### 整体方案视图

支持类似：

```text
方案 A          方案 B          方案 C

¥158,000       ¥149,800        ¥142,500

酒店 A          酒店 A          酒店 B
婚庆 A          婚庆 B          婚庆 B
摄影 B          摄影 A          摄影 A
……
```

快速切换和比较。

### 整体方案差异

能够动态突出：

```text
婚庆

方案 A
¥15,800

      ↓

方案 B
¥13,800

差额
- ¥2,000
```

---

# 13. 漂亮组件扩展原则

不将系统锁死在单一组件库。

基础组件采用：

**shadcn/ui**

但后续允许按需引入 React 生态中的：

* 高级 Card
* Animated Background
* Spotlight
* Gradient Border
* Bento Grid
* Number Animation
* Text Animation
* Carousel
* Timeline
* 3D Card
* Interactive Grid

因为整个项目基于：

```text
React
+
Tailwind
```

绝大多数现代 React UI 组件都可以比较容易集成。

原则：

> **核心组件稳定，视觉组件按需增加。**

不要为了某一个漂亮效果更换整个 UI 框架。

---

# 14. 拖拽与排序

婚礼项目树后续可能需要：

* 自定义排序
* 项目拖动
* 分类调整

建议按实际需求引入：

**dnd-kit**

而不是第一版自行实现复杂拖拽逻辑。

拖拽能力属于增强功能，不影响核心数据模型。

---

# 15. 表单与参数校验

采用：

**Zod**

统一定义：

* 服务端输入校验
* 表单数据结构
* API 参数
* 数据边界

必要时结合：

**React Hook Form**

处理复杂表单。

简单表单不强制使用复杂 Form 框架。

---

# 16. 服务端设计

Next.js 同时作为应用后端。

优先使用：

### Server Components

负责：

* 页面初始数据读取
* 查询
* 服务端渲染

### Server Actions

负责：

* 新增项目
* 修改项目
* 删除项目
* 保存方案
* 选择方案
* 保存整体婚礼方案

例如：

```text
选择摄影方案
      ↓
Server Action
      ↓
Business Service
      ↓
Drizzle
      ↓
SQLite
```

### Route Handlers

只有真正需要 HTTP API 时使用。

例如未来：

* 导出
* 外部系统调用
* 文件下载
* Webhook

普通 CRUD 不需要为了形式统一全部设计 REST API。

---

# 17. 业务逻辑分层

虽然采用 Next.js 单体应用，但禁止把业务逻辑直接堆进页面组件。

推荐结构：

```text
UI
 ↓
Actions
 ↓
Services
 ↓
Repositories
 ↓
Database
```

例如：

```text
WeddingPlanPage

      ↓

saveWeddingPlanAction()

      ↓

WeddingPlanService

      ↓

WeddingPlanRepository

      ↓

Drizzle / SQLite
```

保持：

> **部署单体，代码模块化。**

---

# 18. 数据库

采用：

**SQLite**

原因：

* 无需数据库服务器
* 无需单独部署
* 数据库就是一个文件
* 性能完全满足个人婚礼项目
* 备份简单
* 运维成本极低
* 与 Next.js 单体部署匹配

ORM 使用：

**Drizzle ORM**

Drizzle 官方原生支持 SQLite，包括 `node:sqlite` 等驱动。

---

# 19. 数据量评估

本项目只用于一场婚礼。

即使按照较高估算：

```text
婚礼项目          100
候选方案          500
资源              300
宾客              500
整体方案          100
方案快照明细       10,000+
```

对于 SQLite 仍然属于极小数据量。

实际使用预计远低于此规模。

纯：

* 数字
* 字符串
* 日期
* JSON
* 关系数据

占用空间非常小。

预计数据库长期基本处于：

**几 MB ～ 几十 MB以内**

完全不需要为了数据规模使用 PostgreSQL / MySQL。

---

# 20. 文件存储原则

第一版不建议在数据库中存储：

* 图片二进制
* 视频
* PDF
* 合同
* 大型附件

数据库只记录：

```text
文件名
文件路径
文件类型
关联对象
```

文件本身单独存储。

例如：

```text
/data
   wedding.db

/uploads
   hotel/
   photography/
   wedding-company/
```

这样即使未来大量上传图片：

> 数据库仍然保持很小。

---

# 21. ORM

采用：

**Drizzle ORM**

主要负责：

* Schema
* 查询
* 关系
* Migration
* TypeScript 类型推导

相比大型 ORM，Drizzle 更轻，比较适合：

```text
Next.js
+
TypeScript
+
SQLite
```

这种项目。

---

# 22. 数据迁移

数据库结构变更必须使用 Migration。

禁止上线后直接手工修改 SQLite 表结构。

推荐：

```text
Schema
 ↓
Drizzle Migration
 ↓
SQLite
```

这样后续：

* 新增字段
* 新增资源类型
* 新增方案快照
* 修改数据结构

都有版本记录。

---

# 23. 整体方案快照

整体婚礼方案必须采用：

**Snapshot**

思想。

例如：

当前婚礼：

```text
酒店        A
婚庆        B
摄影        A
摄像        C
```

点击：

> 保存方案 A

系统保存当前所有项目状态及金额。

以后即使当前婚礼修改：

```text
摄影 A
↓
摄影 B
```

已经保存的：

> 方案 A

仍保持原状态。

这样才可以可靠实现：

```text
方案 A
VS
方案 B
VS
方案 C
```

历史整体比较。

---

# 24. 项目建议目录

推荐：

```text
src/
│
├── app/
│   │
│   ├── dashboard/
│   ├── wedding/
│   ├── plans/
│   ├── resources/
│   └── guests/
│
├── components/
│   │
│   ├── ui/
│   ├── wedding/
│   ├── plan/
│   ├── resource/
│   └── dashboard/
│
├── actions/
│
├── server/
│   │
│   ├── services/
│   └── repositories/
│
├── db/
│   │
│   ├── schema/
│   ├── migrations/
│   └── index.ts
│
├── lib/
│   │
│   ├── validations/
│   ├── constants/
│   └── utils/
│
└── types/
```

不为了“企业级”继续拆出几十层目录。

---

# 25. 部署方式

采用：

**Docker**

整体：

```text
Docker
│
└── Next.js Application
        │
        └── SQLite
```

数据目录挂载 Volume：

```text
/app/data
```

保证更新容器时：

> SQLite 数据不会丢失。

第一版无需：

* Kubernetes
* Docker Swarm
* Redis
* Nginx 集群
* 消息队列
* 数据库集群

---

# 26. 登录权限

如果第一版仅两个人使用，可以保持极简。

暂不设计：

* RBAC
* 组织
* 部门
* 多租户
* 权限树

如果只在可信环境使用，第一版甚至可以暂时不做复杂账户体系。

未来需要公网使用时，再增加：

```text
用户
婚礼
成员
```

关系即可。

---

# 27. 响应式设计

虽然主要通过电脑使用，但必须支持：

* Desktop
* Tablet
* Mobile

手机端尤其适合：

* 临时记录酒店价格
* 添加摄影方案
* 查看资源联系方式
* 查看宾客
* 修改当前选择

因此页面从一开始使用响应式布局。

桌面端：

> 左侧项目树 + 右侧内容

移动端：

> 项目树收进 Drawer / Sheet

而不是强行缩小桌面布局。

---

# 28. 第一版不采用的技术

暂不使用：

```text
Java
Spring Boot

.NET

NestJS 独立后端

Vue
Nuxt

PostgreSQL
MySQL

Redis

Kafka

RabbitMQ

Elasticsearch

微服务

Kubernetes
```

这些技术本身没有问题。

只是对于当前项目：

> 带来的工程复杂度远高于实际收益。

---

# 29. 最终技术栈

```text
语言
└── TypeScript

全栈框架
└── Next.js 16

UI
├── React 19
├── shadcn/ui
├── Tailwind CSS 4
├── Motion for React
└── Lucide React

表单 / 校验
├── Zod
└── React Hook Form（按需）

交互增强
└── dnd-kit（按需）

数据库
└── SQLite

ORM
└── Drizzle ORM

部署
└── Docker
```

---

# 30. 技术架构核心结论

本项目不建设传统：

```text
React
      ↓
REST API
      ↓
Java / .NET
      ↓
数据库
```

三层独立系统。

而采用：

```text
          Next.js

 React UI       Server
    │             │
shadcn/ui      Services
Tailwind          │
Motion         Drizzle
                  │
               SQLite
```

形成：

> **一个 TypeScript 工程 + 一个 Next.js 应用 + 一个 SQLite 数据库。**

技术重点不放在复杂后端架构，而放在：

1. 清晰的数据模型；
2. 完整的整体方案快照逻辑；
3. 良好的组件设计；
4. 流畅的交互；
5. 高质量视觉体验。

最终目标是：

> **后端保持简单稳定，前端拥有充分的设计自由度，既容易由个人维护，也能够持续通过 React 生态和 AI Coding 快速迭代。**
